{
  "name": "fearless-outdoorsman-website",
  "private": true,
  "description": "Run dev from repo root so you always start the Fearless Outdoorsman Astro app (not another project on port 3000).",
  "scripts": {
    "dev": "npm run dev --prefix web",
    "build": "npm run build --prefix web",
    "preview": "npm run preview --prefix web"
  }
}
