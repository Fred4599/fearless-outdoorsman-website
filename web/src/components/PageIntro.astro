---
interface Props {
  label: string;
}

const { label } = Astro.props;
---

<div class="mb-6 flex items-center gap-3 md:mb-8">
  <div class="h-px w-8 shrink-0 bg-zinc-300"></div>
  <span class="text-xs font-medium uppercase tracking-widest text-zinc-500">{label}</span>
</div>
